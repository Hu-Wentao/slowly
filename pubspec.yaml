name: slowly
description: A robust utility for debounce, throttle, and mutex (exhaust) execution in Dart.
version: 0.4.3
homepage: "https://github.com/Hu-Wentao/slowly"

environment:
  sdk: ">=2.17.6 <4.0.0"

dependencies:
  meta: ^1.7.0

dev_dependencies:
  test: ^1.21.0
  lints: ^2.0.0
