name: slowly
description: "Debouncing & Throttling: A lightweight Dart package for easily debouncing and throttling function calls to control execution frequency."
version: 0.3.1
homepage: "https://github.com/Hu-Wentao/slowly"

environment:
  sdk: ">=2.17.6 <4.0.0"

dev_dependencies:
  test: ^1.25.0
